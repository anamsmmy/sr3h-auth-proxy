<Window x:Class="MacroApp.Views.AffiliateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="التسويق بالعمولة – اربح من مشاركاتك"
        Width="650" Height="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F5F5F5"
        FontFamily="Segoe UI"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BestBadgeButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FFB300"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF9800"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RedButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FF0000"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CC0000"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2196F3" CornerRadius="8" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="💰 التسويق بالعمولة" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,3"/>
                    <TextBlock Text="اربح من مشاركاتك مع SR3H MACRO" FontSize="12" Foreground="#E3F2FD"/>
                </StackPanel>

                <Button x:Name="CloseButton" Grid.Column="1" Content="✕" Width="35" Height="35" Background="White" Foreground="#2196F3" FontSize="18" FontWeight="Bold" Click="CloseButton_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="17.5" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E0E0E0"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,0,20">
                <Border Background="White" BorderBrush="#FFB300" BorderThickness="0,4,0,0" CornerRadius="8" Padding="20,20" Margin="0,0,0,15">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#FFB300" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                                <TextBlock Text="🏆 الأفضل" FontSize="11" FontWeight="Bold" Foreground="White"/>
                            </Border>

                            <TextBlock Grid.Column="1" Text="شركاء سلة" FontSize="16" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center"/>
                        </Grid>

                        <TextBlock Text="✅ تحصل على عمولتك كاملة (30% = 30%)" FontSize="13" Foreground="#333" Margin="0,0,0,8" TextWrapping="Wrap"/>
                        <TextBlock Text="🔗 رابط تسويق خاص + تطبيق الكود تلقائياً للعميل" FontSize="13" Foreground="#666" Margin="0,0,0,12" TextWrapping="Wrap"/>

                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="SallaVideoButton" Grid.Column="0" Content="▶️ شاهد الشرح" Style="{StaticResource RedButton}" Height="38" FontSize="12" Click="SallaVideoButton_Click"/>

                            <Button x:Name="SallaJoinButton" Grid.Column="2" Content="🔗 الانضمام الآن" Style="{StaticResource BestBadgeButton}" Height="38" FontSize="12" Click="SallaJoinButton_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="White" BorderBrush="#2196F3" BorderThickness="0,4,0,0" CornerRadius="8" Padding="20,20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="منصة كودماب" FontSize="16" FontWeight="Bold" Foreground="#333" Margin="0,0,0,12"/>

                        <TextBlock Text="📊 عند تخصيص 30% للمسوق، يصل للمسوق 21%" FontSize="13" Foreground="#333" Margin="0,0,0,8" TextWrapping="Wrap"/>
                        <TextBlock Text="ℹ️ كودماب تخصم 30% من عمولة المسوق" FontSize="13" Foreground="#666" Margin="0,0,0,12" TextWrapping="Wrap"/>

                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="CodemapVideoButton" Grid.Column="0" Content="▶️ شاهد الشرح" Style="{StaticResource RedButton}" Height="38" FontSize="12" Click="CodemapVideoButton_Click"/>

                            <Button x:Name="CodemapJoinButton" Grid.Column="2" Content="🔗 الانضمام الآن" Style="{StaticResource ModernButton}" Height="38" FontSize="12" Click="CodemapJoinButton_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="#E8F5E9" BorderBrush="#4CAF50" BorderThickness="1" CornerRadius="8" Padding="15,15">
                    <StackPanel>
                        <TextBlock Text="💡 نصائح لزيادة الأرباح" FontSize="13" FontWeight="Bold" Foreground="#2E7D32" Margin="0,0,0,8"/>
                        <TextBlock Text="• شارك رابط التسويق الخاص بك في المنصات الاجتماعية" FontSize="12" Foreground="#333" Margin="0,0,0,5"/>
                        <TextBlock Text="• ركز على جودة المحتوى والشرح الواضح" FontSize="12" Foreground="#333" Margin="0,0,0,5"/>
                        <TextBlock Text="• استفد من الفيديوهات التعليمية المتاحة" FontSize="12" Foreground="#333" Margin="0,0,0,5"/>
                        <TextBlock Text="• تابع أحدث العروض والخصومات" FontSize="12" Foreground="#333"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
