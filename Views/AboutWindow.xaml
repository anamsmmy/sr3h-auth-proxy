<Window x:Class="MacroApp.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="حول التطبيق - SR3H MACRO" 
        Height="550" 
        Width="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        FlowDirection="RightToLeft"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Info Item Style -->
        <Style x:Key="InfoItem" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Feature Item Style -->
        <Style x:Key="FeatureItem" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#2196F3" 
                CornerRadius="8" 
                Padding="20,15">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="📁 حول التطبيق" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,5"/>
                <TextBlock Text="معلومات تفصيلية عن ماكرو سرعة" 
                           FontSize="12" 
                           Foreground="#E3F2FD" 
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="2" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Border Background="White" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Padding="25,20">
                <StackPanel>
                    
                    <!-- App Logo/Icon Section -->
                    <Border Background="Transparent" 
                            CornerRadius="8" 
                            Padding="20" 
                            Margin="0,0,0,20"
                            HorizontalAlignment="Center">
                        <Image Source="pack://application:,,,/logo.png" 
                               Width="450" 
                               Height="220" 
                               Stretch="Uniform"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>

                    <!-- App Information -->
                    <Grid Margin="0,0,0,20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- App Name -->
                        <TextBlock Grid.Row="0" Grid.Column="0" 
                                   Text="اسم التطبيق:" 
                                   FontWeight="Bold" 
                                   Style="{StaticResource InfoItem}"/>
                        <TextBlock Grid.Row="0" Grid.Column="2" 
                                   Text="ماكرو سرعة – SR3H MACRO" 
                                   Style="{StaticResource InfoItem}"/>

                        <!-- Version -->
                        <TextBlock Grid.Row="2" Grid.Column="0" 
                                   Text="النسخة الحالية:" 
                                   FontWeight="Bold" 
                                   Style="{StaticResource InfoItem}"/>
                        <TextBlock Grid.Row="2" Grid.Column="2" 
                                   Text="1.0.1" 
                                   Style="{StaticResource InfoItem}"/>

                        <!-- Release Year -->
                        <TextBlock Grid.Row="4" Grid.Column="0" 
                                   Text="سنة الإصدار:" 
                                   FontWeight="Bold" 
                                   Style="{StaticResource InfoItem}"/>
                        <TextBlock Grid.Row="4" Grid.Column="2" 
                                   Text="2025" 
                                   Style="{StaticResource InfoItem}"/>

                        <!-- Contact Info -->
                        <TextBlock Grid.Row="6" Grid.Column="0" 
                                   Text="البريد الإلكتروني:" 
                                   FontWeight="Bold" 
                                   Style="{StaticResource InfoItem}"/>
                        <TextBlock Grid.Row="6" Grid.Column="2" 
                                   Text="7jgamer.ads@gmail.com" 
                                   Style="{StaticResource InfoItem}"
                                   Foreground="#2196F3"
                                   TextDecorations="Underline"
                                   Cursor="Hand"/>

                        <!-- Website -->
                        <TextBlock Grid.Row="8" Grid.Column="0" 
                                   Text="الموقع الإلكتروني:" 
                                   FontWeight="Bold" 
                                   Style="{StaticResource InfoItem}"/>
                        <TextBlock Grid.Row="8" Grid.Column="2" 
                                   Text="www.SR3H.com" 
                                   Style="{StaticResource InfoItem}"
                                   Foreground="#2196F3"
                                   TextDecorations="Underline"
                                   Cursor="Hand"/>

                        <!-- Developer -->
                        <TextBlock Grid.Row="10" Grid.Column="0" 
                                   Text="المطور:" 
                                   FontWeight="Bold" 
                                   Style="{StaticResource InfoItem}"/>
                        <TextBlock Grid.Row="10" Grid.Column="2" 
                                   Text="فريق سرعة" 
                                   Style="{StaticResource InfoItem}"/>
                    </Grid>

                    <!-- Features Section -->
                    <Border Background="#F8F9FA" 
                            BorderBrush="#E0E0E0" 
                            BorderThickness="1" 
                            CornerRadius="6" 
                            Padding="20,15" 
                            Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="✨ مميزات التطبيق" 
                                       FontSize="16" 
                                       FontWeight="Bold" 
                                       Foreground="#2196F3" 
                                       Margin="0,0,0,15"/>
                            
                            <TextBlock Text="✅ يدعم التسلسل الزمني للمفاتيح" 
                                       Style="{StaticResource FeatureItem}"/>
                            <TextBlock Text="✅ تنفيذ مفاتيح الماكرو بدقة زمنية عالية" 
                                       Style="{StaticResource FeatureItem}"/>
                            <TextBlock Text="✅ حفظ واستيراد الإعدادات" 
                                       Style="{StaticResource FeatureItem}"/>
                            <TextBlock Text="✅ آلية تشغيل وإيقاف ذكية" 
                                       Style="{StaticResource FeatureItem}"/>
                            <TextBlock Text="✅ دعم اللغة العربية الكامل" 
                                       Style="{StaticResource FeatureItem}"/>
                            <TextBlock Text="✅ واجهة مستخدم حديثة ومبسطة" 
                                       Style="{StaticResource FeatureItem}"/>
                            <TextBlock Text="✅ استمرارية العمل في الخلفية" 
                                       Style="{StaticResource FeatureItem}"/>

                        </StackPanel>
                    </Border>

                    <!-- Copyright Section -->
                    <Border Background="#E8F5E8" 
                            BorderBrush="#4CAF50" 
                            BorderThickness="1" 
                            CornerRadius="6" 
                            Padding="15,10">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="© 2025 فريق سرعة - جميع الحقوق محفوظة" 
                                       FontSize="12" 
                                       Foreground="#2E7D32" 
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,5"/>
                            <TextBlock Text="ماكرو سرعة، سر السرعة." 
                                       FontSize="11" 
                                       Foreground="#4CAF50" 
                                       HorizontalAlignment="Center"
                                       FontStyle="Italic"/>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </Border>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center">
            <Button x:Name="YouTubeButton" 
                    Content="📺 شرح يوتيوب" 
                    Style="{StaticResource ModernButton}" 
                    Width="130" 
                    Margin="5"
                    Click="YouTubeButton_Click"/>
            <Button x:Name="PlatformButton" 
                    Content="🌐 منصة سرعة" 
                    Style="{StaticResource ModernButton}" 
                    Width="130" 
                    Margin="5"
                    Click="PlatformButton_Click"/>
            <Button x:Name="TikTokButton" 
                    Content="🎵 تيك توك" 
                    Style="{StaticResource ModernButton}" 
                    Width="130" 
                    Margin="5"
                    Click="TikTokButton_Click"/>
            <Button x:Name="CloseButton" 
                    Content="إغلاق" 
                    Style="{StaticResource ModernButton}" 
                    Width="100" 
                    Margin="5"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>