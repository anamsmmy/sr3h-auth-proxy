<Window x:Class="MacroApp.Views.KeyCaptureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تحديد الزر - Key Selection" 
        Height="380" 
        Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        FlowDirection="RightToLeft"
        KeyDown="Window_KeyDown"
        MouseDown="Window_MouseDown"
        Focusable="True">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- العنوان -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="🎯 تحديد الزر"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#2196F3"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            
            <TextBlock Text="اضغط على أي زر في الكيبورد أو الماوس"
                       FontSize="14"
                       Foreground="#666666"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- المحتوى -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#E0E0E0" 
                BorderThickness="2" 
                CornerRadius="8" 
                Padding="30">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="InstructionText"
                           Text="انتظار الضغط على زر..."
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#333333"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>
                
                <Border Background="#F5F5F5"
                        BorderBrush="#2196F3"
                        BorderThickness="2"
                        CornerRadius="6"
                        Padding="20"
                        MinHeight="60"
                        MinWidth="200"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,20">
                    <TextBlock x:Name="DetectedKeyText"
                               Text=""
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="#2196F3"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextAlignment="Center"/>
                </Border>
                
                <TextBlock Text="الأزرار المدعومة:"
                           FontSize="12"
                           Foreground="#666666"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,5"/>
                
                <TextBlock Text="• جميع أزرار الكيبورد (A-Z, 0-9, F1-F12, Space, Enter, إلخ)"
                           FontSize="10"
                           Foreground="#888888"
                           HorizontalAlignment="Center"/>
                
                <TextBlock Text="• أزرار الماوس (الأيسر، الأيمن، الأوسط، X1, X2)"
                           FontSize="10"
                           Foreground="#888888"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- الأزرار -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,20,0,0">
            <Button x:Name="OkButton"
                    Content="موافق"
                    MinWidth="100"
                    Height="40"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Margin="0,0,10,0"
                    Padding="15,8"
                    IsEnabled="False"
                    Click="OkButton_Click"/>
            
            <Button x:Name="ClearButton"
                    Content="مسح"
                    MinWidth="100"
                    Height="40"
                    Background="#FF9800"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Margin="0,0,10,0"
                    Padding="15,8"
                    Visibility="Collapsed"
                    Click="ClearButton_Click"/>
            
            <Button x:Name="CancelButton"
                    Content="إلغاء"
                    MinWidth="100"
                    Height="40"
                    Background="#F44336"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Padding="15,8"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>