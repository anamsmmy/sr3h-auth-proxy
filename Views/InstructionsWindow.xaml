<Window x:Class="MacroApp.Views.InstructionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تعليمات الاستخدام - SR3H MACRO"
        Width="650" Height="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        FontFamily="Segoe UI"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section Header Style -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- Instruction Item Style -->
        <Style x:Key="InstructionItem" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- Key Button Style -->
        <Style x:Key="KeyButton" TargetType="Border">
            <Setter Property="Background" Value="#E3F2FD"/>
            <Setter Property="BorderBrush" Value="#2196F3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#2196F3" 
                CornerRadius="8" 
                Padding="20,15">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="📖 تعليمات الاستخدام" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,5"/>
                <TextBlock Text="دليل شامل لاستخدام ماكرو سرعة" 
                           FontSize="12" 
                           Foreground="#E3F2FD" 
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="2" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Border Background="White" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Padding="25,20">
                <StackPanel>
                    
                    <!-- Game Settings Section -->
                    <TextBlock Text="🎮 إعدادات اللعبة" Style="{StaticResource SectionHeader}"/>
                    <Border Background="#FFF3E0" 
                            BorderBrush="#FF9800" 
                            BorderThickness="1" 
                            CornerRadius="6" 
                            Padding="15,10" 
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="⚠️ مهم جداً:" 
                                       FontWeight="Bold" 
                                       FontSize="14" 
                                       Foreground="#E65100" 
                                       Margin="0,0,0,5"/>
                            <TextBlock Text="تأكد أن خيار &quot;Auto Confirm Edits&quot; في نافذة &quot;GAME&quot; على &quot;Edit&quot;" 
                                       Style="{StaticResource InstructionItem}"
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>



                    <!-- Delay Setting Section -->
                    <TextBlock Text="⏱️ إعدادات السرعة" Style="{StaticResource SectionHeader}"/>
                    <Border Background="#E8F5E8" 
                            BorderBrush="#4CAF50" 
                            BorderThickness="1" 
                            CornerRadius="6" 
                            Padding="15,10" 
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="الخيار Delay ms:" 
                                       FontWeight="Bold" 
                                       FontSize="14" 
                                       Foreground="#2E7D32" 
                                       Margin="0,0,0,5"/>
                            <TextBlock Text="يتحكم في سرعة استجابة الماكرو (كلما قل الرقم، زادت السرعة)" 
                                       Style="{StaticResource InstructionItem}"/>
                        </StackPanel>
                    </Border>

                    <!-- Usage Steps Section -->
                    <TextBlock Text="📝 خطوات الاستخدام" Style="{StaticResource SectionHeader}"/>
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="1️⃣ اضبط إعدادات اللعبة كما هو موضح أعلاه" 
                                   Style="{StaticResource InstructionItem}"/>
                        <TextBlock Text="2️⃣ حدد المفاتيح المناسبة لكل وظيفة" 
                                   Style="{StaticResource InstructionItem}"/>
                        <TextBlock Text="3️⃣ اضبط سرعة الاستجابة (Delay)" 
                                   Style="{StaticResource InstructionItem}"/>
                        <TextBlock Text="4️⃣ اضغط زر &quot;تطبيق&quot; لحفظ الإعدادات" 
                                   Style="{StaticResource InstructionItem}"/>
                        <TextBlock Text="5️⃣ اضغط زر &quot;تشغيل&quot; لبدء الماكرو" 
                                   Style="{StaticResource InstructionItem}"/>
                    </StackPanel>

                    <!-- Important Notes Section -->
                    <TextBlock Text="⚠️ ملاحظات مهمة" Style="{StaticResource SectionHeader}"/>
                    <Border Background="#FFEBEE" 
                            BorderBrush="#F44336" 
                            BorderThickness="1" 
                            CornerRadius="6" 
                            Padding="15,10">
                        <StackPanel>
                            <TextBlock Text="• تأكد من ضبط الإعدادات حسب مفاتيح لعبتك" 
                                       Style="{StaticResource InstructionItem}"/>
                            <TextBlock Text="• اختبر الماكرو في بيئة آمنة قبل الاستخدام الفعلي" 
                                       Style="{StaticResource InstructionItem}"/>
                            <TextBlock Text="• يمكنك إيقاف الماكرو في أي وقت بالضغط على زر &quot;إيقاف&quot;" 
                                       Style="{StaticResource InstructionItem}"/>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </Border>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center">
            <Button x:Name="CloseButton" 
                    Content="إغلاق" 
                    Style="{StaticResource ModernButton}" 
                    Width="100" 
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>