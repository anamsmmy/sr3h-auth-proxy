<Window x:Class="MacroApp.Views.PropertiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="الاعدادات - SR3H MACRO" 
        Height="688" 
        Width="725"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F8F9FA"
        FlowDirection="RightToLeft"
        Icon="../icon.ico">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InfoCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" BlurRadius="8" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <TextBlock Text="⚙️ الاعدادات" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"
                       Foreground="#2196F3"
                       Margin="0,0,0,5"/>
            <TextBlock Text="خصائص وحالة تطبيق SR3H MACRO" 
                       FontSize="14" 
                       HorizontalAlignment="Center"
                       Foreground="#666666"/>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- حالة التفعيل -->
                <Border Style="{StaticResource InfoCard}">
                    <StackPanel>
                        <TextBlock Text="🔐 حالة التفعيل" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="#4CAF50"
                                   Margin="0,0,0,15"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- العمود الأيمن -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical">
                                <TextBlock Text="حالة التنشيط:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="CurrentStatusText" Text="نشط" Foreground="#4CAF50" FontWeight="Bold"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Vertical">
                                <TextBlock Text="نوع التفعيل:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="ActivationTypeText" Text="تجريبي" Foreground="#2196F3"/>
                            </StackPanel>

                            <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Vertical">
                                <TextBlock Text="البريد:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="35"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="EmailText" Grid.Column="0" Text="-" Foreground="#666666" TextAlignment="Right"/>
                                    <Button Grid.Column="1" Content="👁️" Width="30" Height="25" Background="Transparent" BorderThickness="0" Foreground="#2196F3" Click="ToggleEmail_Click" Cursor="Hand" Padding="0"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel Grid.Row="6" Grid.Column="0" Orientation="Vertical">
                                <TextBlock Text="كود التفعيل:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="35"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="SubscriptionCodeText" Grid.Column="0" Text="-" Foreground="#666666" TextAlignment="Right"/>
                                    <Button Grid.Column="1" Content="👁️" Width="30" Height="25" Background="Transparent" BorderThickness="0" Foreground="#2196F3" Click="ToggleCode_Click" Cursor="Hand" Padding="0"/>
                                </Grid>
                            </StackPanel>

                            <!-- العمود الأيسر -->
                            <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Vertical">
                                <TextBlock Text="تاريخ التفعيل:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="LastActivationText" Text="-" Foreground="#666666" TextAlignment="Right"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Vertical">
                                <TextBlock Text="نهاية التفعيل:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="ExpirationDateText" Text="-" Foreground="#F44336" FontWeight="SemiBold" TextAlignment="Right"/>
                            </StackPanel>

                            <StackPanel Grid.Row="4" Grid.Column="2" Orientation="Vertical">
                                <TextBlock Text="الوقت المتبقي:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="RemainingTimeText" Text="-" Foreground="#FF9800" FontWeight="SemiBold" TextAlignment="Right"/>
                            </StackPanel>

                            <StackPanel Grid.Row="6" Grid.Column="2" Orientation="Vertical">
                                <TextBlock Text="عدد النقلات:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TransferCountText" Text="0/3" Foreground="#2196F3" TextAlignment="Right"/>
                            </StackPanel>

                            <StackPanel Grid.Row="8" Grid.Column="2" Orientation="Vertical">
                                <TextBlock Text="آخر نقل:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="LastTransferText" Text="-" Foreground="#666666" TextAlignment="Right"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- نظام الاشتراك والتفعيل -->
                <Border Style="{StaticResource InfoCard}">
                    <StackPanel>
                        <TextBlock Text="💳 نظام الاشتراك والتفعيل" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="#2196F3"
                                   Margin="0,0,0,15"/>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" FlowDirection="RightToLeft" Margin="0,0,0,10">
                            <Button x:Name="TrialActivationButton" 
                                    Content="📅 تجربة 7 أيام" 
                                    Style="{StaticResource ModernButton}" 
                                    Background="#4CAF50"
                                    Width="160" 
                                    Height="45"
                                    FontSize="12"
                                    FontWeight="SemiBold"
                                    Margin="0,0,10,0"
                                    Click="TrialActivationButton_Click">
                                <Button.ToolTip>
                                    <ToolTip Content="تفعيل نسخة تجريبية مجانية لمدة 7 أيام"/>
                                </Button.ToolTip>
                            </Button>
                            
                            <Button x:Name="CodeActivationButton" 
                                    Content="🔑 تفعيل بالكود" 
                                    Style="{StaticResource ModernButton}" 
                                    Background="#2196F3"
                                    Width="160" 
                                    Height="45"
                                    FontSize="12"
                                    FontWeight="SemiBold"
                                    Margin="0,0,10,0"
                                    Click="CodeActivationButton_Click">
                                <Button.ToolTip>
                                    <ToolTip Content="تفعيل اشتراك باستخدام رمز الترخيص"/>
                                </Button.ToolTip>
                            </Button>
                            
                            <Button x:Name="RebindDeviceButton" 
                                    Content="🔄 إعادة ربط الجهاز" 
                                    Style="{StaticResource ModernButton}" 
                                    Background="#FF9800"
                                    Width="160" 
                                    Height="45"
                                    FontSize="12"
                                    FontWeight="SemiBold"
                                    Click="RebindDeviceButton_Click">
                                <Button.ToolTip>
                                    <ToolTip Content="ربط الاشتراك بجهاز جديد"/>
                                </Button.ToolTip>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>


            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="4" HorizontalAlignment="Center">
            <Button Content="إغلاق" 
                    Style="{StaticResource ModernButton}"
                    Width="120"
                    Height="40"
                    Click="CloseButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>