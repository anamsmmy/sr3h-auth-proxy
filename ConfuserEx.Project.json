{
  "project": {
    "rules": [
      {
        "inherit": true,
        "pattern": "true",
        "settings": {
          "controls": [
            "anti ildasm",
            "anti debug",
            "anti dump",
            "string encryption",
            "base64 encode",
            "constants folding",
            "ctrl flow",
            "call proxy",
            "invalidmetadata"
          ]
        }
      },
      {
        "inherit": true,
        "pattern": "SR3H MACRO",
        "settings": {
          "rename": {
            "mode": "all",
            "password": "SR3H_MACRO_PROTECT_2024_SECURE"
          }
        }
      }
    ],
    "plugins": []
  },
  "packers": [
    {
      "id": "compressor",
      "enabled": true
    }
  ]
}
