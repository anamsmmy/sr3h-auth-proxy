# تعليمات إعداد ماكرو سرعة - SR3H MACRO

## 🚀 الإعداد السريع

### 1. إعداد قاعدة البيانات Supabase

#### أ. إنشاء المشروع
1. اذهب إلى [supabase.com](https://supabase.com)
2. سجل الدخول أو أنشئ حساب جديد
3. أنشئ مشروع جديد باسم "SR3H_MACRO"

#### ب. تنفيذ SQL Script
1. اذهب إلى **SQL Editor** في لوحة التحكم
2. انسخ محتوى ملف `Database/supabase_setup.sql`
3. نفذ الكود بالضغط على **Run**

#### ج. إضافة المستخدمين
```sql
-- إضافة مستخدم جديد
INSERT INTO macro_subscriptions (email, order_id, is_active, subscription_start) VALUES 
('customer@example.com', 'SR3H12345', true, NOW());
```

### 2. بناء التطبيق

#### للتطوير:
```bash
# تشغيل سريع
./run.bat

# أو يدوياً
dotnet restore
dotnet run
```

#### للإنتاج:
```bash
# بناء ملف تنفيذي
./build.bat

# الملف النهائي سيكون في: publish/MacroApp.exe
```

## 🔒 الأمان والحماية

### البيانات المشفرة
- **URL Supabase**: مشفر بـ AES-256 + XOR
- **Service Role Key**: محمي بطبقات تشفير متعددة
- **Hardware ID**: مولد من مكونات الجهاز الأساسية

### الحماية من التلاعب
- فحص العمليات المشبوهة (Cheat Engine, OllyDbg, إلخ)
- التحقق من تكامل التطبيق
- مراقبة المتغيرات البيئية المشبوهة
- تسجيل الأنشطة المشبوهة

## 📋 معلومات المشروع

### البيانات الحساسة (مشفرة في التطبيق)
- **Supabase URL**: `https://vogdhlbcgokhqywyhfbn.supabase.co`
- **Service Role**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

⚠️ **تحذير**: هذه البيانات مشفرة ومحمية في التطبيق. لا تشاركها مع أحد!

### العلامة التجارية
- **الاسم العربي**: ماكرو سرعة
- **الاسم الإنجليزي**: SR3H MACRO
- **الشعار**: ماكرو سرعة، يعطيك السرعة.
- **الموقع**: www.SR3H.com

## 🛠️ استكشاف الأخطاء

### مشاكل التفعيل
```
خطأ: "فشل في تحميل إعدادات الاتصال الآمنة"
الحل: تأكد من أن ملفات التشفير سليمة
```

```
خطأ: "البريد الإلكتروني غير مسجل"
الحل: أضف البريد في قاعدة البيانات:
INSERT INTO email_subscriptions (email, order_id, is_active) VALUES 
('user@example.com', 'ORDER123', true);
```

### مشاكل الأمان
```
تحذير: "تم اكتشاف أدوات تلاعب مشبوهة"
الحل: أغلق جميع برامج التطوير والتحليل قبل تشغيل التطبيق
```

## 📁 هيكل المشروع

```
MACRO_SR3H/
├── Views/                  # واجهات المستخدم
│   ├── MainWindow.xaml     # النافذة الرئيسية
│   ├── LicenseWindow.xaml  # نافذة التفعيل
│   └── ActionEditorWindow.xaml # محرر الإجراءات
├── Services/               # الخدمات
│   ├── AuthenticationService.cs # خدمة التفعيل
│   ├── EncryptionService.cs     # خدمة التشفير
│   ├── SecurityService.cs       # خدمة الأمان
│   ├── MacroExecutionService.cs # تنفيذ الماكرو
│   └── ConfigurationService.cs  # إدارة الإعدادات
├── Models/                 # نماذج البيانات
├── Database/               # ملفات قاعدة البيانات
├── Styles/                 # أنماط التصميم
├── logo.png               # الشعار
├── icon.ico               # الأيقونة
└── README.md              # دليل المستخدم
```

## 🔧 إعدادات متقدمة

### تخصيص فترة السماح Offline
```json
// في appsettings.json
"Authentication": {
  "OfflineGraceDays": 30  // عدد الأيام
}
```

### تخصيص إعدادات الماكرو
```json
"Macro": {
  "DefaultKeySequenceDelay": 10,
  "DefaultActionDuration": 1,
  "MaxActionsPerMacro": 50
}
```

## 📞 الدعم الفني

- **الموقع**: www.SR3H.com
- **البريد**: support@SR3H.com

---
© 2024 ماكرو سرعة - SR3H.com. جميع الحقوق محفوظة.