# دليل إنشاء ملف التثبيت SR3H MACRO v1.0.0

## 📋 المتطلبات الأساسية

### 1. تثبيت Inno Setup
- قم بتحميل Inno Setup 6 من: https://jrsoftware.org/isdl.php
- ثبت البرنامج في المسار الافتراضي: `C:\Program Files (x86)\Inno Setup 6\`

### 2. التحقق من ملفات التطبيق
- تأكد من وجود التطبيق المبني في: `C:\MACRO_SR3H\bin\Release\net6.0-windows\`
- تأكد من وجود جميع ملفات DLL المطلوبة

## 🚀 طرق إنشاء ملف التثبيت

### الطريقة الأولى: استخدام ملف Batch
```bash
# تشغيل ملف البناء التلقائي
C:\MACRO_SR3H\Setup\Build_Installer.bat
```

### الطريقة الثانية: استخدام PowerShell
```powershell
# تشغيل سكريبت PowerShell
C:\MACRO_SR3H\Setup\Build_Installer_v1.0.0.ps1
```

### الطريقة الثالثة: يدوياً باستخدام Inno Setup
```bash
# تشغيل Inno Setup مباشرة
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "C:\MACRO_SR3H\Setup\SR3H_MACRO_Setup_v1.0.0.iss"
```

## 📁 هيكل الملفات المطلوبة

```
C:\MACRO_SR3H\
├── bin\Release\net6.0-windows\
│   ├── SR3H MACRO.exe          # الملف التنفيذي الرئيسي
│   ├── SR3H MACRO.dll          # مكتبة التطبيق
│   ├── *.dll                   # جميع المكتبات المطلوبة
│   ├── *.deps.json             # ملفات التبعيات
│   ├── *.runtimeconfig.json    # إعدادات وقت التشغيل
│   └── runtimes\               # ملفات وقت التشغيل
├── Setup\
│   ├── app_icon.ico            # أيقونة التطبيق
│   ├── app_icon.png            # شعار التطبيق
│   ├── SR3H_MACRO_Setup_v1.0.0.iss  # سكريبت Inno Setup
│   └── Output\                 # مجلد الإخراج
└── اقرأني.txt                  # ملف التعليمات
```

## ⚙️ خصائص ملف التثبيت

### ✅ المميزات المضمنة:
- **دعم اللغة العربية**: واجهة التثبيت باللغة العربية
- **تحقق من .NET 6.0**: يتحقق من وجود .NET Runtime ويثبته إذا لزم الأمر
- **اختصار سطح المكتب**: ينشئ اختصار على سطح المكتب (افتراضياً مفعل)
- **تسجيل في النظام**: يسجل التطبيق في قائمة "إضافة/إزالة البرامج"
- **حماية الأيقونات**: الأيقونات محفوظة في مجلد `Resources` محمي
- **مسار التثبيت**: `C:\Program Files\SR3H MACRO\` (افتراضي)
- **إلغاء التثبيت**: إمكانية إلغاء التثبيت بسهولة

### 🔐 الأمان والحماية:
- **صلاحيات إدارية**: يتطلب صلاحيات إدارية للتثبيت
- **حماية الملفات**: الأيقونات والموارد محمية من التلاعب
- **تشفير الموارد**: الأيقونات مدمجة في ملف التثبيت

### 📊 معلومات التطبيق:
- **الاسم**: SR3H MACRO
- **الإصدار**: 1.0.0
- **الناشر**: منصة سرعة
- **الموقع**: www.SR3H.com
- **البريد**: 7jgamer.ads@gmail.com

## 🧪 اختبار ملف التثبيت

### قبل التوزيع:
1. **اختبر التثبيت** على جهاز نظيف
2. **تحقق من الاختصارات** (سطح المكتب، قائمة ابدأ)
3. **اختبر تشغيل التطبيق** بعد التثبيت
4. **اختبر إلغاء التثبيت** للتأكد من الحذف الكامل
5. **تحقق من قائمة البرامج** في Windows

### متطلبات النظام للمستخدم النهائي:
- **نظام التشغيل**: Windows 10 أو أحدث (64-bit)
- **المعمارية**: x64 فقط
- **الذاكرة**: 512 MB RAM كحد أدنى
- **المساحة**: 100 MB مساحة فارغة
- **.NET Runtime**: 6.0 أو أحدث (يتم تثبيته تلقائياً)

## 📤 التوزيع

### ملف التثبيت النهائي:
- **الاسم**: `SR3H_MACRO_Setup.exe`
- **الموقع**: `C:\MACRO_SR3H\Setup\Output\`
- **الحجم المتوقع**: ~15-25 MB (حسب التبعيات)

### نصائح للتوزيع:
- ✅ اختبر الملف على أجهزة مختلفة
- ✅ تأكد من عمل الرابط لتحميل .NET Runtime
- ✅ وفر تعليمات واضحة للمستخدمين
- ✅ احتفظ بنسخة احتياطية من ملف التثبيت

## 🔧 استكشاف الأخطاء

### مشاكل شائعة وحلولها:

#### خطأ: "لم يتم العثور على Inno Setup"
**الحل**: تأكد من تثبيت Inno Setup 6 في المسار الصحيح

#### خطأ: "ملفات مفقودة"
**الحل**: تأكد من بناء التطبيق بنجاح قبل إنشاء ملف التثبيت

#### خطأ: "فشل في إنشاء ملف التثبيت"
**الحل**: تحقق من صلاحيات الكتابة في مجلد Output

#### خطأ: ".NET Runtime غير موجود"
**الحل**: تأكد من صحة رابط تحميل .NET Runtime في السكريبت

## 📞 الدعم

للحصول على المساعدة:
- **البريد الإلكتروني**: 7jgamer.ads@gmail.com
- **الموقع**: www.SR3H.com

---
© 2025 منصة سرعة - جميع الحقوق محفوظة