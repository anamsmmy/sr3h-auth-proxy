
### خامسًا – نظام التفعيل المعتمد (بدون مفاتيح، فقط بالبريد + HWID):

أعتمد على قاعدة بيانات Supabase لتنفيذ آلية التفعيل التالية:

- أنا أقوم بإدخال بريد المستخدم يدويًا داخل جدول `email_subscriptions`، مع تعيين `is_active = TRUE`.
- حقل `hardware_id` متروك فارغًا في البداية.
- عند أول تشغيل للتطبيق، يُطلب من المستخدم إدخال بريده فقط.
- يقوم التطبيق بتوليد HWID فريد من الجهاز (ويُفضّل أن يعتمد على مجموعة من الخصائص مثل: CPU ID + Disk Serial + MAC Address).
- يرسل التطبيق البريد + HWID إلى Supabase للتحقق.

منطق التحقق:
1. إذا كان البريد غير موجود أو غير مفعل: يتم رفض الطلب برسالة واضحة.
2. إذا كان البريد موجودًا ومفعلًا، ولم يُربط بعد بـ HWID: تتم اضافة HWID الحالي بشكل تلقائي في `hardware_id`
3. إذا كان البريد مرتبطًا مسبقًا بـ HWID:
   - إذا تطابق HWID الحالي: يتم السماح.
   - إذا اختلف كليًا: يتم الرفض برسالة "الاشتراك مرتبط بجهاز آخر".

في حال رفض التفعيل:
- يظهر للمستخدم خيار **"إعادة ربط الاشتراك"**.
- يُطلب من المستخدم إدخال:
  1. البريد الإلكتروني
  2. رقم الطلب (يُزوّد به بعد الشراء من منصة سرعة)
- في حال تطابقت البيانات مع سجل موجود في الجدول (البريد + رقم الطلب):
  - يتم تعيين حقل `hardware_id` إلى `NULL` تلقائيًا.
  - وتتم اضافة HWID الجديد، تلقائيا في حقل `hardware_id` وكأنه مستخدم جديد مفعل.

#### 📝 ملاحظات:
- يجب إضافة عمود جديد في جدول `email_subscriptions` باسم `order_id` (نوع `text`) لتخزين رقم الطلب.
- يتم حفظ التفعيل مشفرًا محليًا، مع فترة سماح Offline (مثلاً 30 يوم)، وبعد انتهائها يتم إعادة التحقق تلقائيًا.