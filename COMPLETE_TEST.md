# 🧪 اختبار شامل - نظام التفعيل الجديد

## ✅ قائمة التحقق الكاملة

### 1. 🗄️ قاعدة البيانات
- [x] جدول `macro_subscriptions` جديد
- [x] حقول: email, hardware_id, is_active, subscription_start, order_id, last_check, notes
- [x] دالة `authenticate_user(user_email, user_hardware_id)`
- [x] دالة `verify_authentication(user_email, user_hardware_id)`
- [x] دالة `reactivate_subscription(user_email, user_order_id)`

### 2. 🔧 الخدمات المحدثة
- [x] `AuthenticationService.cs` - منطق التفعيل الجديد
- [x] `HardwareIdService.cs` - توليد Hardware ID معقد
- [x] `EncryptionService.cs` - تشفير البيانات الحساسة
- [x] `SecurityService.cs` - فحص الأمان
- [x] `ConfigurationService.cs` - إدارة الإعدادات

### 3. 🎨 واجهة المستخدم
- [x] `LicenseWindow.xaml` - نافذة التفعيل محدثة
- [x] `MainWindow.xaml` - العلامة التجارية الجديدة
- [x] دعم إعادة ربط الجهاز
- [x] رسائل خطأ واضحة

### 4. 🔐 الأمان والحماية
- [x] تشفير بيانات Supabase
- [x] Hardware ID من CPU + MAC + HDD + BIOS
- [x] حفظ محلي مشفر
- [x] فترة سماح 30 يوم offline

## 🎯 سيناريوهات الاختبار

### السيناريو 1: تفعيل جديد
```
1. المستخدم يدخل: test@sr3h.com
2. النظام يولد Hardware ID تلقائياً
3. يرسل إلى authenticate_user()
4. النتيجة المتوقعة: "تم تفعيل الاشتراك بنجاح"
```

### السيناريو 2: تفعيل موجود (نفس الجهاز)
```
1. المستخدم يدخل: test@sr3h.com (مرة ثانية)
2. النظام يولد نفس Hardware ID
3. يرسل إلى authenticate_user()
4. النتيجة المتوقعة: "تم التحقق من الاشتراك بنجاح"
```

### السيناريو 3: جهاز مختلف
```
1. المستخدم يدخل: test@sr3h.com (من جهاز آخر)
2. النظام يولد Hardware ID مختلف
3. يرسل إلى authenticate_user()
4. النتيجة المتوقعة: "الاشتراك مرتبط بجهاز آخر"
5. يظهر زر "إعادة ربط الجهاز"
```

### السيناريو 4: إعادة ربط الجهاز
```
1. المستخدم يضغط "إعادة ربط الجهاز"
2. يدخل البريد: test@sr3h.com
3. يدخل رقم الطلب: SR3H001
4. يرسل إلى reactivate_subscription()
5. النتيجة المتوقعة: "تم إعادة ربط الاشتراك بنجاح"
6. يعود للشاشة الرئيسية
7. يحاول التفعيل مرة أخرى → نجاح
```

### السيناريو 5: بريد غير موجود
```
1. المستخدم يدخل: notfound@example.com
2. يرسل إلى authenticate_user()
3. النتيجة المتوقعة: "البريد الإلكتروني غير مسجل أو غير مفعل"
```

### السيناريو 6: اشتراك غير مفعل
```
1. في قاعدة البيانات: is_active = false
2. المستخدم يدخل البريد
3. النتيجة المتوقعة: "الاشتراك غير مفعل"
```

## 🔬 اختبارات تقنية

### اختبار Hardware ID
```csharp
// يجب أن يولد نفس الـ ID في كل مرة على نفس الجهاز
var id1 = HardwareIdService.GenerateHardwareId();
var id2 = HardwareIdService.GenerateHardwareId();
Assert.AreEqual(id1, id2);
```

### اختبار التشفير
```csharp
// يجب أن يشفر ويفك التشفير بنجاح
var original = "test data";
var encrypted = EncryptionService.Encrypt(original);
var decrypted = EncryptionService.Decrypt(encrypted);
Assert.AreEqual(original, decrypted);
```

### اختبار الأمان
```csharp
// يجب أن يكتشف البيئات المشبوهة
var securityCheck = SecurityService.PerformSecurityCheck();
Assert.IsTrue(securityCheck.IsOverallSecure);
```

## 📋 قائمة التحقق قبل النشر

### قاعدة البيانات
- [ ] تم تنفيذ `Database/supabase_setup.sql`
- [ ] تم إضافة مستخدم للاختبار
- [ ] تم اختبار جميع الدوال

### التطبيق
- [ ] تم تشغيل `test_new_auth.bat` بنجاح
- [ ] تم اختبار جميع السيناريوهات
- [ ] تم اختبار إعادة ربط الجهاز
- [ ] تم اختبار العمل offline

### الأمان
- [ ] بيانات Supabase مشفرة ومخفية
- [ ] Hardware ID يعمل بشكل صحيح
- [ ] فحص الأمان يعمل
- [ ] التشفير المحلي يعمل

### الواجهة
- [ ] العلامة التجارية صحيحة
- [ ] الرسائل باللغة العربية
- [ ] التصميم متناسق
- [ ] جميع الأزرار تعمل

## 🚀 أوامر الاختبار السريع

```bash
# اختبار شامل
./test_new_auth.bat

# بناء للإنتاج
./build.bat

# تشغيل للتطوير
./run.bat

# اختبار عادي
dotnet test
```

## 📊 بيانات الاختبار

### في Supabase:
```sql
-- مستخدم نشط
INSERT INTO macro_subscriptions (
    email, order_id, is_active, subscription_start
) VALUES (
    'test@sr3h.com', 'SR3H001', true, NOW()
);

-- مستخدم غير مفعل
INSERT INTO macro_subscriptions (
    email, order_id, is_active, subscription_start
) VALUES (
    'inactive@sr3h.com', 'SR3H002', false, NOW()
);
```

## ✅ معايير النجاح

### التفعيل الأساسي:
- ✅ يقبل البريد الصحيح
- ✅ يرفض البريد غير الموجود
- ✅ يرفض الاشتراك غير المفعل
- ✅ يحفظ Hardware ID في أول مرة
- ✅ يتذكر الجهاز في المرات التالية

### إعادة الربط:
- ✅ يرفض الجهاز المختلف
- ✅ يظهر خيار إعادة الربط
- ✅ يقبل البريد ورقم الطلب الصحيح
- ✅ يعيد تعيين Hardware ID
- ✅ يسمح بالتفعيل على الجهاز الجديد

### الأمان:
- ✅ بيانات Supabase محمية
- ✅ Hardware ID معقد وفريد
- ✅ البيانات المحلية مشفرة
- ✅ يعمل offline لمدة 30 يوم

---
**🎯 النظام جاهز للاختبار الشامل!**  
**ماكرو سرعة، يعطيك السرعة!** 🚀